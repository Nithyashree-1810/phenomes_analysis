def extract_phonemes(word: str):
    """Future: return IPA phonemes for the word."""
    return []

def detect_weak_phonemes(mistakes: list):
    """
    Future-ready function.

    For now, always return empty list so system never breaks.
    """
    return []

def analyze_weak_phonemes(mistakes: list):
    """
    Wrapper for compatibility with existing code.
    """
    return detect_weak_phonemes(mistakes)